@page "/studentedit/{id}"

<h2>Edit</h2>
<h4>Student</h4>
<span class="border-bottom"></span>
<EditForm Model="@Student" OnValidSubmit="HandleValidSubmit">
    <DataAnnotationsValidator />
    <div class="form-group row">
        <label for="firstName" class="col-sm-2 col-form-label">
            NIM
        </label>
        <div class="col-sm-10">
            <InputText id="studentId" class="form-control" placeholder="NIM" @bind-Value="Student.studentId" disabled/>
        </div>
    </div>
    <div class="form-group row">
        <label for="firstName" class="col-sm-2 col-form-label">
            First Name
        </label>
        <div class="col-sm-10">
            <InputText id="firstName" class="form-control" placeholder="First Name" @bind-Value="Student.firstName"/>
            <ValidationMessage For="@(()=>Student.firstName)"/>
        </div>
    </div>
    <div class="form-group row">
        <label for="lastName" class="col-sm-2 col-form-label">
            Last Name
        </label>
        <div class="col-sm-10">s
            <InputText id="lastName" class="form-control" placeholder="Last Name" @bind-Value="Student.lastName"/>
            <ValidationMessage For="@(()=>Student.lastName)"/>
        </div>
    </div>
    <div class="form-group row">
        <label for="enrollmentDate" class="col-sm-2 col-form-label">Tanggal Masuk</label>
        <div class="col-sm-10">
            <InputDate @bind-Value="Student.enrollmentDate" class="form-control" />
        </div>
    </div>
    <button class="btn btn-primary" type="submit">Edit</button>
</EditForm>